# End to End Computer_Vision

- Create a Project Folder in local Machine.

- Open using Vs code or pycharm.

- Creating a Environment using or opening Terminal.

```cmd
conda create -p cvdev python==3.9
``` 

```cmd
conda activate cvdev/
``` 

- make sure working only in activated, New Environment(mlvenv).


🔗 https://github.com/

- Create a New repository in GitHub

- follow the Git hub instrustions in to local Machine. 

## Git commands or Git Instructions to be followed in local Machine.

- git init

- git add .

- git status

- git commit -m "initial commit"

- git branch -M main

- git remote add origin <copied link from github>

- git push -u origin main.

## Imp: Create a addfiles like .gitignore then make python as gitignore == default in GitHub then pull it in local machine by command

- git pull
 
- Imp: Before installing the requirements.txt, create a setup.py file.

## 🔗 requirements.txt

 -opencv-python
 -e .


 ```cmd
pip install -r requirements.txt
```